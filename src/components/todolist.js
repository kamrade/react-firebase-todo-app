import React, { Component } from 'react';
import './todolist.css';

var TodoList = React.createClass({
  render: function(){
    return(
      <ul>

      </ul>
    );
  }
});

export default TodoList;
